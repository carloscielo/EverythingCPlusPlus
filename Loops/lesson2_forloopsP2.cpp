#include <iostream>
#include <cmath>
using namespace std;

int main() {

    for(int i=0;i<10;i++){
        cout<<i<<endl;
    };

    for(int i = 1; i<3;i++){
        cout<<"happy new year! \n";
    };

    //the following for loop is one which counts down for example
    for(int x = 10; x>=0;x--){
        cout<<"T-"<<x<<"\n"<<endl;
    };
    //for loops are only really used when you need to move 
    // a fixed or set amount of times


    return 0;
}